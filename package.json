{
	"name": "handoff-plugin",
	"version": "0.1.0",
	"private": true,
	"description": "Handoff plugin: compact observer + independent handoff writer + rules injection + tool-call recorder",
	"type": "module",
	"scripts": {
		"typecheck": "tsc --noEmit"
	},
	"exports": {
		".": "./src/index.ts"
	},
	"cline": {
		"plugins": [
			{
				"paths": [
					"./src/index.ts"
				],
				"capabilities": [
					"messageBuilders",
					"rules",
					"hooks"
				]
			}
		]
	},
	"peerDependencies": {
		"@cline/core": "*",
		"@cline/shared": "*"
	},
	"peerDependenciesMeta": {
		"@cline/core": {
			"optional": true
		},
		"@cline/shared": {
			"optional": true
		}
	}
}
